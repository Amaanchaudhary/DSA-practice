var strStr = function (haystack, needle) {
    // indexOf is a string method which returns -1 if the given string not found in another string and if found it will return the first letter index of first occurence
    return haystack.indexOf(needle)
};



console.log(strStr("anmaan", 'an'));
